node_modules/
.DS_Store
.env
.env.*
!.env.example
npm-debug.log*
# Hand-kept clip log; lives in the MEDIA_TRACKER_SEED_B64 secret, never in git
data/media-tracker-seed.json
# Clip-email rows; only the encrypted data/clip-emails.json.gpg is committed
data/clip-emails.json

# Local Worker dev secrets
.dev.vars

# Private model evaluations include withheld human tracker labels.
artifacts/jev-evaluation/
